# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Tests / outils
.pytest_cache/
.cache/

# Sorties générées à l'exécution (rapports QA/KPI, logs)
reports/
*.log

# Masters reconstruits localement (le master canonique vient de l'ETL)
data_clean/master_schedule_rebuilt.csv
data_clean/optimization/

# Données source volumineuses (ne pas versionner — fournies hors dépôt)
Asignacion_*.xlsx

# Sorties générées à l'exécution (plannings, snapshots de versions)
outputs/
versions/

# Sous-produit régénérable de professor_credits.py (assignation complète)
professor_assignment.csv

# OS
.DS_Store
Thumbs.db
